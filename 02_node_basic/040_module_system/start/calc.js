function plus(a, b) {
  return a + b;
}

function minus(a, b) {
  return a - b;
}

// module.exports = {
//   // plus: plus,
//   // minus: minus,
// };
  
// パラメーターと値（関数）が同じなら省略できる。
module.exports = {
  plus,
  minus
};